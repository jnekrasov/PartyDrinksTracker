# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'

target 'PartyDrinksTracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PartyDrinksTracker
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  target 'PartyDrinksTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PartyDrinksTrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
